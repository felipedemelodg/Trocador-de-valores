const campoComOvalor = document.getElementById('valor')
const botaoTrocar = document.getElementById('botao-que-troca')
const sessaoResultado = document.getElementById('resultado')
const formularios = document.getElementById('formulario')
export const elem = {
    campoComOvalor,
    botaoTrocar,
    sessaoResultado,
    formularios
}